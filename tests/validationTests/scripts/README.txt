
To run the validation tests, executing the following command in you shell:

$ python run.py

Note that the tests are extensive and can therefore take a while.

Make sure you have all python packages installed needed by run.py.
In particular you may have to execute:

$ conda install h5py
$ conda install pyaml
$ conda install scikit-image


